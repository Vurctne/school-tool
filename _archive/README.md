# `_archive/` — Quarantined / pending-cleanup artifacts

Created 2026-04-25 as part of the Orchestrator + Sub-agent reorganisation Step 3 (context compression).

This directory holds **historical or stray artifacts that aren't part of active development** but are kept for reference instead of deleted outright. Sub-agents should not read from here for context — `CONTEXT-SUMMARY.md` is the canonical sub-agent front-loaded context.

---

## What lives here

Currently empty (apart from this README). The folder is set up so that future archive moves don't require directory creation each time.

---

## Pending-cleanup queue

Items that should be removed from the live tree at the next convenient Windows-side housekeeping pass. None are urgent; all are low-impact.

| Path | Bytes | Origin | Why archive/delete |
| --- | --- | --- | --- |
| `tools/sub_program/__test_write_probe__.txt` | 9 | Bash sandbox-permission probe written by orchestrator on 2026-04-25 | Stray; pollutes package directory; got committed in baseline `c95097c`. PowerShell: `Remove-Item tools\sub_program\__test_write_probe__.txt` |
| `school_finance_toolkit.egg-info/`, `school_tool.egg-info/` | small | Auto-regenerated by `pip install -e` | Both are gitignored already; only need to delete on demand if they confuse tooling. |

If a pending item becomes worth keeping (e.g. someone realises a stray file actually has audit value), the right move is to **`mv` it into `_archive/` with a one-line note added to "What lives here" above** instead of deleting.

---

## What this directory is NOT for

- Old design references — those live in `design_system/`.
- Previous-round handoff docs (`handoff/phase1_drift_map.md`, `handoff/phase3_final_report.md`) — those are active reference material, not archive.
- Auto-generated caches (`.pytest_cache/`, `.ruff_cache/`, `.mypy_cache/`) — those are gitignored and self-managing.
- The user's running-idea stub (formerly root `Tools_in_Development.md`) — already merged into `docs/Tools_in_Development.md` Exploratory section; the stub itself was deleted, not archived.
