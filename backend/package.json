{
  "name": "school-tool-backend",
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "tsc --noEmit",
    "build": "tsc",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest run"
  },
  "dependencies": {
    "hono": "^4.5.0",
    "zod": "^3.23.0",
    "hash-wasm": "^4.11.0",
    "@noble/ed25519": "^2.1.0",
    "resend": "^3.5.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "@cloudflare/vitest-pool-workers": "^0.5.0",
    "@cloudflare/workers-types": "^4.20240718.0",
    "wrangler": "^3.78.0"
  }
}
